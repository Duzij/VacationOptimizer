server {
    listen 80;
    server_name longvacation.eu;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name longvacation.eu;

    ssl_certificate /etc/letsencrypt/live/longvacation.eu/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/longvacation.eu/privkey.pem;

    include /etc/nginx/includes/default.conf;
}

# Redirect HTTP
server {
    listen 80;
    server_name optimize-vacation-for.me www.optimize-vacation-for.me;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://longvacation.eu$request_uri;
    }
}

# Redirect HTTPS
server {
    listen 443 ssl;
    server_name optimize-vacation-for.me www.optimize-vacation-for.me;

    ssl_certificate /etc/letsencrypt/live/optimize-vacation-for.me/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/optimize-vacation-for.me/privkey.pem;

    return 301 https://longvacation.eu$request_uri;
}